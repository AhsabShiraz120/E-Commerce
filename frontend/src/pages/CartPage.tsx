export function CartPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-2">Your Cart</h1>
      <p className="text-muted">Line items + quantity stepper + Order Summary land on branch 12.</p>
    </div>
  );
}
